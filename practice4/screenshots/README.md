# Скриншоты для практики 4

Разместите здесь **не меньше трёх** изображений дашборда (и при желании один кадр из Prometheus).

Рекомендуемый набор:

| Файл | Содержание |
|------|------------|
| `grafana_load_1.png` | Grafana: после нагрузки в основном на `/api/health` (рост RPS по 200; бизнес-панель может быть у нуля). |
| `grafana_load_2.png` | Grafana: после создания транзакций — видны бизнес-метрики и при необходимости изменение p95. |
| `prometheus_targets.png` | Prometheus: **Status → Targets**, scrape gateway/worker в состоянии **UP**. |

Имена должны совпадать с путями в [`PRACTICE4.md`](../PRACTICE4.md) (встроенные `![...](./screenshots/...)`). При других именах файлов обновите ссылки в `PRACTICE4.md`.
