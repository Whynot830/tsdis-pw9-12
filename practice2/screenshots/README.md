# Скриншоты для `PRACTICE2.md`

В **`practice2/screenshots/`** лежат три PNG, на которые ссылается [`PRACTICE2.md`](../PRACTICE2.md):

| Файл | Назначение |
|------|------------|
| **`autotests_gateway.png`** | Успешный `npm test` в **gateway** (`practice2/services/gateway`). |
| **`autotests_worker.png`** | Успешный `npm test` в **worker** (`practice2/services/worker`). |
| **`docker_compose.png`** | Логи / статус `docker compose` (`postgres`, `gateway`, `worker`, при необходимости `db-migrate`). |

Имена лучше не менять — в `PRACTICE2.md` зашиты пути `./screenshots/autotests_gateway.png`, `./screenshots/autotests_worker.png`, `./screenshots/docker_compose.png`.
